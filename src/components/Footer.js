import React from 'react';

const Footer = (props) => {
    return (
        <div>
            <p>So any {props.boss}, any {props.bosses} or any {props.bigBoss} would like to hire me please contact me.</p>
        </div>
    )
}

export default Footer;