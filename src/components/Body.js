import React from 'react';

const Body = (props) => {
    return (
        <div>
        <p>I am a {props.job}, learning {props.code} and {props.cyber}</p>
        </div>
    )
}

export default Body;